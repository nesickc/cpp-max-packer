# T-001: Windows CPU build and dependency lock

Branch: `feature/AT-01-windows-cpu-build`. Base: merged `main` at `067f1e52be1cbb21f2b58dc0de37745bc34c70e8` (PR #1). Status: implemented and locally verified; hosted CI pending.

Requirements: SYS-01 and SYS-02; build groundwork for AT-01. [ADR 0003](../spec/decisions/0003-pinned-cpu-build.md) defines the accepted boundary.

Deliver a pinned native CPU build, real dependency integration checks, checked toolchain/dependency records, third-party notices and a staged diagnostic with runtime-dependency evidence. Preserve the foundation tests and original STL fixtures. Keep production schemas, CLI/service behavior and geometry/packing algorithms in their subsequent tasks.

Completion requires:

- Fresh Debug and Release builds with the selected static x64 toolchain and dependency graph.
- Passing analytic Manifold/FCL/meshoptimizer/pocketfft/JSON checks and retained foundation tests.
- Observed red/green tests for profile, dependency and runtime-audit guards.
- A staged Release diagnostic that runs independently of development paths and passes the dependency audit.
- Bounded reference timing evidence, resolved code-review findings, and an actual passing hosted Windows CI run.

Local verification on 2026-09-08:

- Both fresh wrapper builds passed with the locked local profile: 14/14 native cases in Debug and Release. The five real dependency cases supplement nine foundation cases.
- All 27 Python tests passed; all nine focused guard tests passed again after admitting the observed, signed Windows `apphelp.dll`. The audit still rejects arbitrary DLLs in system directories.
- Guard regressions reproduced incorrect handling of inactive vcpkg records, valid `-MTd` flags and inconsistent runtime module names/paths before fixes. The new compiler-record CLI option's initial parser rejection is not counted as behavioral red evidence.
- The Release diagnostic passed all five integration cases from `%TEMP%/spectrapack-t001-cpu-package/diagnostics`, outside the checkout, with only Windows directories on `PATH`. Its retained PE imports and loaded-module report pass the runtime audit. Original copyright notices and lock/build metadata are staged alongside it.
- Two bounded reference timing workloads passed with five measured samples and one warmup under a 60-second watchdog. These are informational test-reference timings, not packing performance; no comparison to QA-01's different CRT is claimed.
- Astra Extra High reviewed the native integration, guards, documentation and CI. Reported implementation findings were corrected; actual hosted CI remains the outstanding qualification gate.

Full local evidence is retained in `.local/t001/`: `native-{debug,release}-final.log`, `python-final.log`, `expert-red.log`, `expert-green.log`, `expert-apphelp-{red,green}.log`, `stage-final.log`, `diagnostic-final.log`, `runtime-final.json`, `pe-imports-final.log`, `audit-final.log` and `reference-final.json`. CI uploads corresponding logs, measured dependency/build records, notices and the staged package. See [build instructions](../docs/BUILDING.md) for reproducible commands.

T-002 waits for this feature's merge. T-001 does not complete M0 or the full installation/packing acceptance gate AT-01.
