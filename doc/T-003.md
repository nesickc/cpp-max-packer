# T-003: STL import and accepted geometry

Branch: `feature/AT-03-stl-import`. Base: merged T-002/PR #3 at
`6af862e3c978b16447ba13758d45bdf044b767e7`. Status: implementation, local
qualification and Astra review complete; user review and merge pending.

Requirements: GEO-01–GEO-03, spec §§5.1–5.2 and the `inspect` command in §8.1.
AT-03/AT-04 supply the import and repair cases. T-004 retains independent
placement/containment/clearance validation; T-005 retains display LODs and fields.

This ticket introduces a native import boundary that preserves source bytes,
requires explicit units, records object/container frames, diagnoses the full mesh,
and controls creation of immutable accepted geometry. Safe exact cleanup is
reported. Tolerance welding is an explicit bounded proposal whose acceptance is
separate from inspection. Invalid or indeterminate geometry cannot become an
accepted solid.

## Observable cases

- Equivalent ASCII/binary cubes, misleading binary `solid` headers, multiple
  components, negative coordinates and Unicode paths.
- Millimeters, the 25.4 mm inch cube, positive custom scales, and independent
  source-to-local reconstruction for objects and containers.
- Structured failures for malformed/truncated data, nonfinite coordinates and
  resource limits; source bytes and hashes remain unchanged.
- Reported exact cleanup, reversed winding, open/non-manifold meshes,
  self-intersection, touching/overlapping shells, preserved nested cavities, and
  a small seam requiring explicitly accepted welding.
- Practical diagnostics for all ten pinned `rc/` assets, with acceptance outcomes
  established by the importer rather than inferred from closed-edge counts.
- A bounded import timing workload with fixed input, correctness checks, build
  metadata and a watchdog. It establishes no packing-performance claim.

## Performance workload

Run all ten manifest-pinned files through the public inspection command once,
checking source hashes, byte/triangle counts, original bounds, frame mapping and
the actual acceptance verdict. The six analytic box containers require valid
acceptance and their independently computed box volumes. Item verdicts are pinned
only after diagnostic review; a defect is retained as a regression case.

Measure three Release inspections of the largest source (`pryanik_2.STL`) after
one warmup. Retain each sample, median and nearest-rank p95, source/executable
hashes, build metadata, machine identity and work counters. Use a per-process
watchdog and an overall bounded run; timing is initially informational, with no
cross-machine regression percentage or invented solver target. Every timed sample
must reproduce the reviewed diagnostic outcome and complete checks. A timeout or
changed outcome fails the workload rather than becoming a timing sample.

Raw red/green, kernel-characterization and timing evidence stays in `.local/t003/`.
Shared interfaces and serialization decisions will be recorded before behavioral
implementation. Routine changes should avoid expanding generated JSON fixtures;
small native fixture builders provide analytic expectations.

## Development evidence

Local Debug and Release pass **95 registered CTest cases each**. The staged
package passes all eight import process cases, service tests and the CPU runtime
audit with developer directories removed from `PATH`. Both reference workloads
and the bounded service timing workload pass. Python checks pass **40 tests**;
all **31 shared contract fixtures**, generated C++/TypeScript freshness and
TypeScript compile checks pass. Astra Extra High approved the final native/CLI,
numerical and benchmark-harness changes.

Logs under `.local/t003/`: `native-debug-volume-final.log`,
`native-release-final.log`, `staged-import-final.log`, `staged-protocol-final.log`,
`runtime-audit-final.log`, `python-completion.log` and `contracts-completion.log`.
The feature's hosted Windows workflow repeats these checks and retains the full
inspection artifacts and bounded timing reports. The ready PR requires successful
CI for its exact feature head; user review/testing/merge remains the delivery gate.

The test-first repairs include source-clobber/hardlink/Unicode failures, omitted
BVH pairs (24 instead of the independently enumerated 54 for a cube), rounded
cavity samples, malformed signs, split work budgets, uncounted welding scans and
lost repair provenance. The CLI seam case verifies invalid-to-valid explicit
repair, nonzero displacement, the accepted candidate hash and unchanged source.
The performance harness has real regressions for omitted facets, incorrect build
metadata, physical-result drift and expired deadlines.

Numerical red/green cases include unsupported floating modes, nonfinite interval
bounds, truncated volume limbs, a large integer tetrahedron and a thin cavity.
Final material volume now uses one correctly rounded conversion of the exact
oriented determinant sum divided by six. An independent 6,032-case rational review
covered rounding ties, subnormals, overflow and signs without a mismatch. See
`predicate-env-review-{red,green}.log`, `volume-rounding-red.log` and
`source-volume-gate-{red,green}.log` for retained reproductions.

The original seven-byte parser fixture and a later command selecting zero tests
are **not** TDD evidence. The valid replacement is `import-phase2-red.log` (6 cases,
37 assertions, one failed leading-plus assertion); `kernel-cube-red.log` separately
reproduces missing solid-analysis assertions. The interruption audit remains in
`reset-audit.md`.

## Reviewed practical outcomes

[Import expectations](../tests/fixtures/import-expectations.json) bind all ten
public outcomes to the unchanged source inventory. **Nine solids are accepted**:
the six containers, both full Pryanik items and Ulamok. All topology, intersection
and containment stages complete. Source and lossless PLY facet coverage are
independently checked; all nine reported volumes exactly match the independently
computed signed determinant sum over the original STL coordinates.

`pryanik_2` preserves a 123,372-face outer shell and a 15,840-face inward cavity.
Independent connectivity and exact ray crossings support its parent/depth record;
material volume is 8430.004424364888 mm³. `pryanik_simplified` is **invalid**, with
13 self-intersection pairs. An independent exact coplanar calculation confirms
positive overlap between original faces 643 and 3143; this is a diagnostic
regression, not an accepted packing input.

The initial 500-million work allowance left the large items indeterminate; the
retained `practical-gate-red.log` rejects those results. Profiling exposed a hash
distribution defect and costly exact predicates. The corrected hash and certified
interval filters improve runtime without weakening validity or work accounting.
The final public work totals are 620,516,986 and 1,009,948,927; the calibrated
1.3-billion default retains over 25% margin and explicit smaller-cap rejection
tests. [ADR 0005](../spec/decisions/0005-stl-import-and-acceptance.md) records the
numerical proof obligations, work accounting and compatibility boundary.

The final local Release qualification (`.local/t003/import-qualification.json`,
`local-windows-2026` profile) passes all ten reviewed outcomes and all timing
repetitions. After one warmup, the largest item takes 6263.789, 6255.112 and
6220.914 ms: median **6255.112 ms**, nearest-rank p95 **6263.789 ms**. These are
whole public inspection process timings; the report retains executable/source
hashes, build and host metadata, raw samples and work counters. Timings are
informational, not a cross-machine threshold or a packing performance claim.

Placement/clearance validation, asynchronous service imports, LODs, packing,
portable projects/exports and desktop integration remain later work. T-003 does
not complete M0/M1 or full AT-14. T-004 waits for this feature's merge.
