# Project status

Updated: 2026-09-08.

- **Current tranche:** QA-01 test foundation on `feature/QA-01-test-foundation`, implemented and locally verified; Astra Extra High review findings are resolved. Awaiting user change request/testing/merge. Delivery follows one task per feature branch and waits for prerequisite merges; see [queue](DELIVERY.md). Agent routing remains as recorded in [AGENT_SETUP.md](AGENT_SETUP.md).
- **Architecture/build:** [initial boundaries](ARCHITECTURE.md), [ADR 0001](../spec/decisions/0001-initial-architecture.md) and [test-foundation ADR 0002](../spec/decisions/0002-test-foundation.md) are recorded. C++20/CMake/Ninja Debug and Release test builds use pinned Catch2/vcpkg. Product dependencies and schemas remain pending; M0 is not complete.
- **Fixtures/tests:** 10 user-supplied STLs (six containers, four items), 228,668 triangles, pinned in [rc-manifest.json](../tests/fixtures/rc-manifest.json). Units are millimeters; containers intend usable closed volumes. All 18 Python checks and nine native reference tests in both fresh Debug/Release configurations pass locally. Two bounded Release reference workloads and an explicit compatible-baseline comparison pass. [Hosted Windows CI passed](https://github.com/nesickc/cpp-max-packer/actions/runs/34267341679) for implementation head `18fc3e7`. [Evidence](TEST_FOUNDATION_EVIDENCE.md) distinguishes tool checks from product acceptance. Solid validity remains unverified; see [test plan](../tests/TEST_PLAN.md).
- **Product state:** no packing engine or desktop implementation. M0–M6 gates are not complete; CPU/GPU qualification and packing benchmarks have not run. Fixture-tool tests do not establish product AT success.
- **Next product milestone:** after the user merges QA-01, begin M0 `T-001` (complete product build/dependency groundwork), followed by `T-002` (schema package, CLI/service envelopes and asset IDs), per [spec §11.1](spectrapack-spec.md#111-milestones). Do not start dependent branches before prerequisite merges.
- **Canonical spec:** `doc/spectrapack-spec.md`, with TDD and user-confirmed fixture conventions added to §11.3. `spec/decisions/` now holds architecture decisions; source documents remain at their existing paths.

Keep this note short. Record completed scope and its evidence, the next bounded task, and unresolved decisions. Link detailed logs instead of copying them. Do not mark a product requirement complete without its acceptance evidence.
