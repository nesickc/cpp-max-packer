# Project status

Updated: 2026-09-12.

- **Current tranche:** T-003 merged through [PR #4](https://github.com/nesickc/cpp-max-packer/pull/4) at `08d094b`. [T-004 independent solid validator](T-004.md) is implemented, locally qualified and reviewed by Astra Extra High on `feature/AT-06-solid-validator`. A ready PR requires hosted qualification of the pushed commit; user review/merge remains the delivery boundary. Dependent work waits for prerequisite merges; see [queue](DELIVERY.md). Agent routing remains as recorded in [AGENT_SETUP.md](AGENT_SETUP.md).
- **Architecture/build:** [initial boundaries](ARCHITECTURE.md), [CPU build ADR 0003](../spec/decisions/0003-pinned-cpu-build.md) and [contract ADR 0004](../spec/decisions/0004-versioned-contracts.md) are recorded. The C++20 static CPU build pins its geometry, compute and JSON/schema dependencies with checked toolchain/dependency records and upstream notices. [T-001 hosted Windows CI passed](https://github.com/nesickc/cpp-max-packer/actions/runs/34276058437) on `c828f2d`; [build instructions](../docs/BUILDING.md). M0 remains incomplete.
- **Fixtures/tests:** T-004 passes 53 Python tests, 31 shared contract fixtures/generated-type checks, and 129 native tests in each Debug/Release build. Staged CLI/stdio/runtime and all ten import regressions pass. The validation matrix checks 91 fixed workloads with one warmup and three measured runs each; all 364 verdicts and snapshots match expectations. Source bytes remain unchanged; nine supplied solids are accepted and the simplified Pryanik remains rejected for 13 self-intersection pairs. [T-004 evidence and limits](T-004.md), [reviewed import expectations](../tests/fixtures/import-expectations.json), [test plan](../tests/TEST_PLAN.md).
- **Product state:** versioned contracts, semantic validation, generated TypeScript, asset identities, capabilities CLI/stdio, source-preserving STL import/diagnostics and explicit repair acceptance are implemented. T-004 adds independent native overlap/containment/clearance validation and immutable valid snapshots. Unresolved geometry remains indeterminate and cannot publish. Packing, jobs, project/export operations and desktop remain pending. M0–M6 and full AT-13/AT-14 gates are not complete.
- **Next acceptance:** after T-004 merges, T-005 adds LOD and conservative fields with representation-isolation tests. T-004 does not establish full AT-08 search behavior or completion of M1; [spec §11.1](spectrapack-spec.md#111-milestones) retains those gates.
- **Canonical spec:** `doc/spectrapack-spec.md`, with TDD and user-confirmed fixture conventions added to §11.3. `spec/decisions/` now holds architecture decisions; source documents remain at their existing paths.

Keep this note short. Record completed scope and its evidence, the next bounded task, and unresolved decisions. Link detailed logs instead of copying them. Do not mark a product requirement complete without its acceptance evidence.
