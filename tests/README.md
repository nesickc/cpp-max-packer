# Fixture checks

Run `python -m unittest discover -s tests -p 'test_*.py'` (or the bundled interpreter at `C:\Users\Nes\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`). These tests cover the inventory helper and pinned reference manifest only; C++ implementation and product acceptance gates remain planned in `tests/TEST_PLAN.md`.
