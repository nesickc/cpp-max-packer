add_executable(spectrapack_reference_benchmark native/reference_benchmark.cpp)
target_link_libraries(spectrapack_reference_benchmark PRIVATE spectrapack_test_support)
target_compile_features(spectrapack_reference_benchmark PRIVATE cxx_std_20)

set_target_properties(spectrapack_reference_benchmark PROPERTIES
  RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin"
)

add_executable(spectrapack_validation_benchmark native/validation_benchmark.cpp)
target_link_libraries(spectrapack_validation_benchmark PRIVATE pack_geometry spectrapack_dependency_json)
target_compile_features(spectrapack_validation_benchmark PRIVATE cxx_std_20)
set_target_properties(spectrapack_validation_benchmark PROPERTIES
  RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin"
)

add_executable(spectrapack_representation_benchmark native/representation_benchmark.cpp)
target_link_libraries(spectrapack_representation_benchmark PRIVATE
  pack_geometry spectrapack_dependency_json
)
target_compile_features(spectrapack_representation_benchmark PRIVATE cxx_std_20)
if(WIN32)
  target_link_libraries(spectrapack_representation_benchmark PRIVATE Psapi)
endif()
set_target_properties(spectrapack_representation_benchmark PROPERTIES
  RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin"
)
